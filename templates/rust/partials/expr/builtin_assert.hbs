{ let {{rust_ident rust_assert_condition_name}} = {{#each args}}{{#unless @index}}{{> expr this}}{{/unless}}{{/each}}; let {{rust_ident rust_assert_message_name}} = {{#each args}}{{#if @index}}{{> expr this}}{{/if}}{{/each}}; if !{{rust_ident rust_assert_condition_name}} { use std::io::Write as _; let mut {{rust_ident rust_assert_stderr_name}} = std::io::stderr().lock(); {{rust_ident rust_assert_stderr_name}}.write_all({{rust_ident rust_assert_message_name}}.as_bytes()).expect("failed to write stderr"); {{rust_ident rust_assert_stderr_name}}.write_all(b"\n").expect("failed to write stderr"); std::process::exit(1); } }
