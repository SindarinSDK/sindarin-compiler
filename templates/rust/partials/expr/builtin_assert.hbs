{ let __sn_assert_condition = {{#each args}}{{#unless @index}}{{> expr this}}{{/unless}}{{/each}}; let __sn_assert_message = {{#each args}}{{#if @index}}{{> expr this}}{{/if}}{{/each}}; if !__sn_assert_condition { use std::io::Write as _; let mut __sn_assert_stderr = std::io::stderr().lock(); __sn_assert_stderr.write_all(__sn_assert_message.as_bytes()).expect("failed to write stderr"); __sn_assert_stderr.write_all(b"\n").expect("failed to write stderr"); std::process::exit(1); } }
