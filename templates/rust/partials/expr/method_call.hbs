{{#if rust_receiver_prefix}}{ {{#each rust_receiver_prefix}}{{> stmt this}} {{/each}}let {{rust_ident rust_receiver_name}} = {{#if rust_receiver_mutating}}&mut{{else}}&{{/if}} ({{> expr object}}); ({{rust_ident rust_receiver_name}}).{{rust_ident method_name}}({{#each args}}{{#if @index}}, {{/if}}{{> expr_resolved_call_arg this}}{{/each}}) }{{else}}{{#if is_static}}{{rust_ident struct_type.name}}::{{rust_ident method_name}}({{#each args}}{{#if @index}}, {{/if}}{{> expr_resolved_call_arg this}}{{/each}}){{else}}({{> expr object}}).{{rust_ident method_name}}({{#each args}}{{#if @index}}, {{/if}}{{> expr_resolved_call_arg this}}{{/each}}){{/if}}{{/if~}}
