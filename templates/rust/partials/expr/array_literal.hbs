{{#if rust_flatten}}{ let mut __sn_array: {{rust_type type}} = Vec::new(); {{#each elements}}{{#if (eq kind "spread")}}__sn_array.extend(({{> expr operand}}).iter().cloned()); {{else}}{{#if (eq kind "range")}}__sn_array.extend({{> expr this}}); {{else}}__sn_array.push({{> expr this}}{{rust_clone_suffix this source_is_borrow}}); {{/if}}{{/if}}{{/each}}__sn_array }{{else}}vec![{{#each elements}}{{#if @index}}, {{/if}}{{> expr this}}{{rust_clone_suffix this source_is_borrow}}{{/each}}]{{/if}}