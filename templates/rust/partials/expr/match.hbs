match {{> expr subject}} {
{{#each arms}}
{{#if is_else}}
    _ => { {{#each body.statements}}{{> expr this.expr}}{{/each}} },
{{else}}
    {{#each patterns}}{{#if @index}} | {{/if}}{{> expr this}}{{/each}} => { {{#each body.statements}}{{> expr this.expr}}{{/each}} },
{{/if}}
{{/each}}
}