{{#if (eq handle.kind "sync_list")}}{ {{#each handle.elements}}if let Some({{rust_ident rust_temp_handle}}) = {{rust_ident rust_thread_handle}}.take() { {{rust_ident name}} = {{rust_ident rust_temp_handle}}.join(); } {{/each}} }{{else}}{{#if rust_thread_handle}}{ if let Some({{rust_ident rust_temp_handle}}) = {{rust_ident rust_thread_handle}}.take() { {{rust_ident handle.name}} = {{rust_ident rust_temp_handle}}.join(); } {{rust_ident handle.name}}.clone() }{{else}}({{> expr handle}}).join(){{/if}}{{/if}}
