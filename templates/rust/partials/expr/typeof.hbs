TypeInfo { name: SnString::from({{rust_string_literal type_name}}), fields: vec![{{#each fields}}{{#if @index}}, {{/if}}FieldInfo { name: SnString::from({{rust_string_literal name}}), typeName: SnString::from({{rust_string_literal type_name}}), typeId: {{type_id}} }{{/each}}], fieldCount: {{field_count}}, typeId: {{type_id}} }
