impl {{name}} {
{{#each methods}}
    fn {{name}}({{#if is_static}}{{#each params}}{{#if @index}}, {{/if}}{{name}}: {{rust_type type}}{{/each}}{{else}}&mut self{{#each params}}, {{name}}: {{rust_type type}}{{/each}}{{/if}}){{#if (eq return_type.kind "void")}}{{else}} -> {{rust_type return_type}}{{/if}} {
{{#each body}}
        {{> stmt this}}
{{/each}}
    }

{{/each}}
}
