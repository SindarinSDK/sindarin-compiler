#![allow(dead_code, unused_mut, unused_variables, unused_parens)]

{{#each structs}}{{> struct this}}{{nl}}{{/each}}{{#if structs}}{{nl}}{{/if}}{{#each functions}}{{#unless is_native}}{{> function this}}{{#unless @last}}{{nl}}{{/unless}}{{/unless}}{{/each}}