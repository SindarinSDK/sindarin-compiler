({
        {{c_type type}} __match_result__;
        {{c_type subject.type}} __match_subject__ = {{> expr subject}};
        {{#each arms}}{{#if is_else}} else {
            __match_result__ = {{#each body.statements}}{{> expr this.expr}}{{/each}};
        }{{else}}{{#if @first}}if{{else}} else if{{/if}} ({{#each patterns}}{{#if @index}} || {{/if}}__match_subject__ == {{> expr this}}{{/each}}) {
            __match_result__ = {{#each body.statements}}{{> expr this.expr}}{{/each}};
        }{{/if}}{{/each}}
        __match_result__;
    })