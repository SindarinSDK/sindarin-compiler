{{c_type return_type}} __sn__{{name}}({{#each params}}{{#if @index}}, {{/if}}{{c_type type}}{{#if (eq mem_qual "as_ref")}} *{{else}} {{/if}}__sn__{{name}}{{/each}}) {
{{#each body}}
    {{> stmt this}}
{{/each}}
}
