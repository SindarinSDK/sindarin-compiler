{
    sn_auto_{{type.name}} {{c_type type}} __sn__{{name}} = {{> expr initializer}};
{{> stmt body}}
    {{#if type.dispose_alias}}{{type.dispose_alias}}{{else}}__sn__{{type.name}}_dispose{{/if}}({{#if type.pass_self_by_ref}}__sn__{{name}}{{else}}&__sn__{{name}}{{/if}});
}
