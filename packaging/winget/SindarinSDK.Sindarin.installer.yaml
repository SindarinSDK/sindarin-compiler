# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
PackageIdentifier: SindarinSDK.Sindarin
PackageVersion: "${VERSION}"
Platform:
  - Windows.Desktop
MinimumOSVersion: 10.0.0.0
InstallerType: zip
NestedInstallerType: portable
NestedInstallerFiles:
  - RelativeFilePath: sindarin-${VERSION}-windows-x64\bin\sn.exe
    PortableCommandAlias: sn
Dependencies:
  PackageDependencies:
    - PackageIdentifier: MartinStorsjo.LLVM-MinGW.UCRT
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/RealOrko/sindarin/releases/download/${TAG}/sindarin-${VERSION}-windows-x64.zip
    InstallerSha256: "${SHA256}"
ManifestType: installer
ManifestVersion: 1.6.0
