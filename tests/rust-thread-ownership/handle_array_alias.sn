fn compute(value: int): int =>
  return value * 2
fn main(): void =>
  var handles: int[] = {}
  handles.push(& compute(21))
  var copied: int[] = handles
  copied !
  println(copied[0])
  handles = {}
