// Test: multi-line array literals should parse correctly

fn main(): void =>
  var nums: int[] = {
    10,
    20,
    30,
    40
  }
  println($"len={nums.length} sum={nums[0] + nums[1] + nums[2] + nums[3]}")
