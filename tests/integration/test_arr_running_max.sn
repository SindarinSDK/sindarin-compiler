fn main(): void =>
    var arr: int[] = {3, 1, 4, 1, 5, 9, 2, 6}
    var maxes: int[] = {arr[0]}
    for var i: int = 1; i < arr.length; i++ =>
        if arr[i] > maxes[i-1] =>
            maxes.push(arr[i])
        else =>
            maxes.push(maxes[i-1])
    print($"{maxes.join(" ")}\n")
