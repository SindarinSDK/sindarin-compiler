fn main(): void =>
    var arr: int[] = {1, 2, 3, 4, 5}
    var result: int[] = {}
    var triple: fn(int): int = fn(x: int): int => x * 3
    for var i: int = 0; i < arr.length; i++ =>
        result.push(triple(arr[i]))
    print($"{result.join(" ")}\n")
