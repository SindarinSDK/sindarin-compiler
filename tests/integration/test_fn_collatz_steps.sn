fn main(): void =>
    print($"{collatz(1)}\n")
    print($"{collatz(6)}\n")

fn collatz(n: int): int =>
    var steps: int = 0
    while n != 1 =>
        if n % 2 == 0 =>
            n /= 2
        else =>
            n = 3 * n + 1
        steps++
    return steps
