// Test: nested multi-line expressions

fn add(a: int, b: int): int => a + b

fn mul(a: int, b: int): int => a * b

fn main(): void =>
  var result: int = add(
    mul(
      3,
      4
    ),
    mul(
      5,
      6
    )
  )
  println($"result: {result}")
