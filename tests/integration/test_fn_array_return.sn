fn main(): void =>
    var arr: int[] = make_range(1, 5)
    print($"{arr.join(", ")}\n")

fn make_range(start: int, count: int): int[] =>
    var result: int[] = {}
    for var i: int = 0; i < count; i++ =>
        result.push(start + i)
    return result
