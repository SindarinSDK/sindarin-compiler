fn main(): void =>
    var arr: int[] = {1, 2, 3, 4, 5}
    var prefix: int[] = {arr[0]}
    for var i: int = 1; i < arr.length; i++ =>
        prefix.push(prefix[i-1] + arr[i])
    print($"{prefix.join(" ")}\n")
