fn main(): void =>
    var arr: int[] = {1, 2, 3, 4, 5, 6, 7, 8}
    var evens: int[] = filter_evens(arr)
    print($"{evens.join(", ")}\n")

fn filter_evens(arr: int[]): int[] =>
    var result: int[] = {}
    for x in arr =>
        if x % 2 == 0 =>
            result.push(x)
    return result
