fn main(): void =>
    var arr: int[] = {5, 12, 8, 3, 17, 9}
    var min: int = arr[0]
    for var i: int = 1; i < arr.length; i++ =>
        if arr[i] < min =>
            min = arr[i]
    print($"{min}\n")
