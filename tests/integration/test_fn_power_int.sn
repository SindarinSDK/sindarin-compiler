fn main(): void =>
    print($"{power(2, 10)}\n")
    print($"{power(3, 4)}\n")

fn power(base: int, exp: int): int =>
    var result: int = 1
    for var i: int = 0; i < exp; i++ =>
        result *= base
    return result
