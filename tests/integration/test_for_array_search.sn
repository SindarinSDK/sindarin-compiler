fn main(): void =>
    var arr: int[] = {5, 12, 8, 3, 17, 9}
    var max: int = arr[0]
    for var i: int = 1; i < arr.length; i++ =>
        if arr[i] > max =>
            max = arr[i]
    print($"{max}\n")
