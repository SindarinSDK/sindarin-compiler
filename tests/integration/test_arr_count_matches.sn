fn main(): void =>
    var arr: int[] = {1, 2, 1, 3, 1, 4, 1}
    var count: int = 0
    for var i: int = 0; i < arr.length; i++ =>
        if arr[i] == 1 =>
            count++
    print($"{count}\n")
