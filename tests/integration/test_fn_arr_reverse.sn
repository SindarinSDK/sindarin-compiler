fn main(): void =>
    var arr: int[] = {1, 2, 3, 4, 5}
    var rev: int[] = reverse_arr(arr)
    print($"{rev.join(" ")}\n")

fn reverse_arr(arr: int[]): int[] =>
    var result: int[] = {}
    for var i: int = arr.length - 1; i >= 0; i-- =>
        result.push(arr[i])
    return result
