fn main(): void =>
    var arr: int[] = {1, 2, 1, 3, 2, 1, 4, 2}
    var count1: int = 0
    var count2: int = 0
    for var i: int = 0; i < arr.length; i++ =>
        if arr[i] == 1 =>
            count1++
        if arr[i] == 2 =>
            count2++
    print($"{count1} {count2}\n")
