fn main(): void =>
    var count: int = 0
    for var i: int = 0; i < 5; i++ =>
        for var j: int = 0; j < 5; j++ =>
            if (i + j) % 2 == 0 =>
                continue
            count++
    print($"{count}\n")
