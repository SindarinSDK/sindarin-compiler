fn main(): void =>
    print($"{find_first_even({1, 3, 5, 4, 6})}\n")
    print($"{find_first_even({1, 3, 5})}\n")

fn find_first_even(arr: int[]): int =>
    for x in arr =>
        if x % 2 == 0 =>
            return x
    return -1
