fn main(): void =>
    var arr: int[] = {1, 2, 3, 4, 5, 6, 7}
    var odds: int[] = {}
    for var i: int = 0; i < arr.length; i++ =>
        if arr[i] % 2 != 0 =>
            odds.push(arr[i])
    print($"{odds.join(" ")}\n")
