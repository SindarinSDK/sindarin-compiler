fn main(): void =>
    var arr: int[] = {10, 15, 13, 20, 18}
    var diffs: int[] = {}
    for var i: int = 1; i < arr.length; i++ =>
        diffs.push(arr[i] - arr[i-1])
    print($"{diffs.join(" ")}\n")
