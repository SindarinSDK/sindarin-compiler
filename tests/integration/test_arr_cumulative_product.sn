fn main(): void =>
    var arr: int[] = {1, 2, 3, 4, 5}
    var result: int[] = {arr[0]}
    for var i: int = 1; i < arr.length; i++ =>
        result.push(result[i-1] * arr[i])
    print($"{result.join(" ")}\n")
