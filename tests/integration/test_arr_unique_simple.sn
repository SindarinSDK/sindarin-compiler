fn main(): void =>
    var arr: int[] = {1, 2, 2, 3, 3, 3, 4}
    var unique: int[] = {}
    for var i: int = 0; i < arr.length; i++ =>
        if !unique.contains(arr[i]) =>
            unique.push(arr[i])
    print($"{unique.join(" ")}\n")
