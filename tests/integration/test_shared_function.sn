// Test shared function modifier
// Shared functions use the caller's arena context

shared fn helper(): int =>
    return 42

shared fn compute(): int =>
    var x: int = helper()
    return x + 10

fn main(): void =>
    var result: int = compute()
    print($"Result: {result}\n")
