# Error test: Cannot dereference opaque types

# Declare opaque type
type OpaqueHandle = opaque

# This should fail: trying to dereference opaque type
native fn bad_deref(h: *OpaqueHandle): OpaqueHandle =>
    return valueOf(h)

fn main(): int =>
    return 0
