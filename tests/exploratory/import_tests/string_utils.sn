// string_utils.sn - Utility string functions for import testing

fn greet(name: str): str =>
  return "Hello, " + name + "!"

fn shout(text: str): str =>
  return text.toUpper() + "!"

fn format_number(n: int): str =>
  return $"Number: {n}"
