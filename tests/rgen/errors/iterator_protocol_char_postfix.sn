struct CharIterator =>
  remaining: int

  fn iter(): CharIterator =>
    return self

  fn hasNext(): bool =>
    return self.remaining > 0

  fn next(): char =>
    self.remaining -= 1
    return 'a'

fn main(): void =>
  var source: CharIterator = { remaining: 1 }
  for value in source =>
    value++
