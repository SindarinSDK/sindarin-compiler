struct Point =>
  x: int

  fn increment(other: Point as ref): void =>
    other.x = other.x + 1

fn main(): void =>
  var point: Point = { x: 1 }
  var other: Point = { x: 2 }
  point.increment(other)
  println(other.x)
