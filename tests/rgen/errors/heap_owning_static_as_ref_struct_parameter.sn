struct Label =>
  text: str

  static fn rename(label: Label as ref): void =>
    label.text = "changed"

fn main(): void =>
  var label: Label = { text: "original" }
  Label.rename(label)
  println(label.text)
