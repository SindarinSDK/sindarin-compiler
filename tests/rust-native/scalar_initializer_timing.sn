fn initialValue(): int =>
  println("initializer")
  return 40

var helperSeed: int = initialValue()

fn helper(value: int): int =>
  return value + helperSeed

native fn throughNative(value: int): int =>
  return helper(value)

fn main(): int =>
  println("body-before-native")
  println(throughNative(2))
  println(throughNative(3))
  return 0
