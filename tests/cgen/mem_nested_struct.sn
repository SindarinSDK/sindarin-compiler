struct Address as ref =>
  city: str

struct Person as ref =>
  name: str
  addr: Address

fn main(): void =>
  var a: Address = {city: "NYC"}
  var p: Person = {
    name: "Alice",
    addr: a
  }
  assert(p.name.length == 5, "name should be Alice")
